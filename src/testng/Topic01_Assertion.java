package testng;

import org.testng.Assert;

public class Topic01_Assertion {
    public static void main(String[] args) {
        //testng cung cấp 3 hàm chính để kiểm tra tính đúng đắn của dữ liệu
        //Kiểm tra dữ liệu nó phải đúng
        //Assert.assertTrue();
        //Kiểm tra dữ liệu nó phải sai
        //Assert.assertFalse();
        //Kiểm tra dữ liệu nó bằng với mong đợi (actual-expected)
        //Assert.assertEquals();
    }
}
